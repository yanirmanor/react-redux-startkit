import { expect } from 'chai'

describe('global testing', function () {
	
	describe('check if testing is working', function () {
		
		it('should pass', function () {
			expect(true).to.be.true;
		});
		
	});
 
});

